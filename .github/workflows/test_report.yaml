on:
  push:
    branches: main

name: rmarkdown

jobs:
  build:
    runs-on: ubuntu-latest
    env:
#      EMAIL: ${{ secrets.EMAIL }}
      GITHUB_PAT: ${{ secrets.GH_PAT }}
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Setup R
        uses: r-lib/actions/setup-r@master

      - name: Install pandoc
        run: |
          brew install pandoc
#      - name: Cache Renv packages
#        uses: actions/cache@v1
#        with:
#          path: $HOME/.local/share/renv
#          key: r-${{ hashFiles('renv.lock') }}
#          restore-keys: r-

#      - name: Cache bookdown results
#        uses: actions/cache@v1
#        with:
#          path: _bookdown_files
#          key: bookdown-${{ hashFiles('**/*Rmd') }}
#          restore-keys: bookdown-

      - name: Install renv and here
        run: |
          sudo R CMD javareconf
          R -e 'install.packages(c("renv", "here"))'
#         R -e 'renv::restore()'

      - name: Build site
        run: Rscript -e 'source(here::here("bookdown/render", "render_bookdown_ghaction.R"))'
#
#       - name: Install npm
#         uses: actions/setup-node@v1
#      - name: Cleanup files
#        run: Rscript -e 'source("remove_files.R")'

      - name: Deploy to github
        uses: JamesIves/github-pages-deploy-action@3.7.1
        with:
#          EMAIL: ${{ secrets.EMAIL }}
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
#          REPOSITORY_NAME: esp_data_aggregation
          BRANCH: gh-pages # The branch the action should deploy to.
          FOLDER: . # The folder the action should deploy.
          TARGET_FOLDER: .
          CLEAN: true # Automatically remove deleted files from the deploy branch
